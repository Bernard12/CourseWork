#include <stdio.h>
#include <stdlib.h>


int* create( int number, int random, int* max); //Done, not tested

void re_size(int* l, int flag, int* max); //Done, not tested

void insert(int* l, int val, int pos,int* max); //Done, not tested

int del(int* l, int pos,int* max); //Done, not tested

void print(int* l); //Done, not tested
